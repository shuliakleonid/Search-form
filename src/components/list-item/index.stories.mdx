<!--- index.stories.mdx -->


import {Canvas, Meta, Story} from '@storybook/addon-docs/blocks';
import ListItem from '.';

<Meta title="Components/ListItem" component={ListItem}/>

# ListItem

With `MDX` we can define a story for `ListItem` right in the middle of our
Markdown documentation.

<!--- This is your Story template function, shown here in React -->

export const Template = (args) => <ListItem {...args} />

<Canvas>
  <Story name="Default" args={{
    label: 'Default story',
    id: 'https://id.com/1'
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
